{{#each posts as |item|}}
<div class="col s12 m6">
  <div class="card">
    <div class="card-content grey-text">
      <span class="card-title"><a href="/post/{{item.id}}">{{item.title}}</a></span>
        <p class="black-text">{{item.content}}</p>
    </div>
    <div class="card-action right-align">
      <a href="#" class="grey-text center-align">Posted By: {{user.name}}</a>
      <a href="#" class="grey-text center-align"> Post Date: {{format_date date_created}}</a>
    </div>
  </div>
</div>
{{/each}}





